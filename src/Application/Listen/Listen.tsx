import React, { useEffect } from "react";
import { ActivityConstructorProps } from "../../types/structureTypes";

const Listen: React.FC<ActivityConstructorProps> = () => {

    return (
        <>
        </>
    );
};

export default Listen;
