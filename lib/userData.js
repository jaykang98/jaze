class previousUserData() {
    let username;
    String data[][][][] ;


    constructor(String args[]){
        this.username   =username;
        this.timeStamp  =timeStamp;
    }
    private userSince;

    get userName()  {   return this.userName;  }
    get timeStamp() {   return this.timeStamp; }
    get userSince() {   return this.userSince; }

    function userSince(username) {}
}

class newUserData extends previousUserData{

}
class storedQuery {
    constructor
}

class firstDegreeData {
    String allData[][][][];

    constructor(String args[][][][]){
        this.allData[][][][]=args[][][][];
    }

    int getArtistScrobbles(String artist) {
        return allData[artist][][][].size;
    }
    int getAlbumScrobbles(String args[artist,album]) {
        return allData[artist][album][][].size;
    }
    int getTrackScrobbles(String[artist,album,track]) {
        return allData[artist][album][track][].size;
    }
}

class secondDegreeData extends firstDegreeData{
    int
}
    function
        for (int i=1; i<args[][][].size; i++) {
            //[artist][album][track][timestamp]

        }
    }

}